import React from "react";
import { Alert } from "react-bootstrap";

const NotFound = () => {
  return (
    <Alert style={{ margin: 30 }} variant="danger">
      Page Not Found
    </Alert>
  );
};

export default NotFound;
